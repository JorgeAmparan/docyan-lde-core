"use client";

import { Expediente } from "./_expediente";

/** CoDos — expediente esquemático del CoDo activo (focal canvas del experto). */
export default function CodosPage() {
  return <Expediente />;
}
